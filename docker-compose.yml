services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: barquiz
      POSTGRES_PASSWORD: barquiz
      POSTGRES_DB: barquiz
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
